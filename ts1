多行字符串
字符串模板
参数类型可以使用冒号
类型推断机制
any  随便类型
number数字类型
boolean
void 方法返回值类型，不需要
方法参数也可以声明类型

可以自己自定义参数类型

给参数指定默认值，带默认值的参数要放在最后面

可选参数  ？

rest and spread操作符  用来声明任意数量的参数
两个用法


generator函数
手工暂停恢复代码
方法前加一个*
yield断点  next方法

析构表达式  从对象属性找给变量 大括号   针对数组用 中括号
方法的参数可以当析构表达式


箭头表达式  用来声明匿名函数，消除传统匿名函数的this指针问题


for of可以被打断

类
访问控制符
public       内外都行
private     内部才行
proected  内部和子内

构造函数  consructor 外部没法访问，只有使用new的时候才可以用
约束，明确要写访问控制符

泛型
规定数组只能放person类型



接口   
用来建立某种代码约定
interface
一个方法的参数类型 声明属性
implements必须要实现借口
声明方法

模块
分割为可重用的单元，给别人或者内部用  
一个文件就是一个模块
export 对外暴露
import 引入模块里面的

注解
加说明，指定框架用

类型定义文件

enum枚举





